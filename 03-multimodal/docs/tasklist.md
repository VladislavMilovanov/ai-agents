# Tasklist — итерационный план разработки бота

## 📊 Отчет по прогрессу

| Итерация | Статус | Что добавлено | Проверка бота |
|---|---|---|---|
| 1 | ✅ Готово | Базовые ответы на `/start`, `/help` и простые сообщения | Проверено: ответы на `/start` и обычное сообщение возвращаются |
| 2 | ✅ Готово | Единый `Settings` из env с валидацией обязательных параметров на старте | Проверено тестами: валидный env проходит, невалидный — ошибка при инициализации |
| 3 | ✅ Готово | Извлечение транзакции из текста, сохранение ключевых полей и подтверждение пользователю | Проверено тестами: `test_handle_text_saves_transaction`, smoke-сценарий |
| 4 | ✅ Готово | Прием фото чека, извлечение транзакции и подтверждение сохранения | Проверено тестом: `test_handle_image_saves_transaction` |
| 5 | ✅ Готово | Формирование отчета с доходами/расходами/балансом и категориями по периодам | Проверено тестами: `test_handle_text_returns_report`, `test_build_report_has_expected_period_title` |
| 6 | ✅ Готово | In-memory контекст диалога для уточнений пользователя | Проверено тестом: `test_handle_text_uses_context_for_clarification` |
| 7 | ✅ Готово | Обработка невалидного ввода и безопасные fallback-ответы при сбоях | Проверено тестами: `test_handle_text_unknown_action`, `test_handle_text_returns_fallback_on_llm_error`, `test_handle_image_returns_validation_message_on_value_error` |
| 8 | ✅ Готово | Сквозная проверка MVP и обновленные инструкции запуска/проверки | Проверено: smoke-тест + раздел "Сквозная проверка MVP" в README |

**Статусы:** ⬜ Не начато · 🔄 В работе · ✅ Готово · ⏸️ Пауза

---

## Итерация 1 — Базовый запуск и роль ассистента
- [x] Бот запускается и отвечает на простое текстовое сообщение
- [x] Есть базовые команды старта/помощи и понятное позиционирование финансового ассистента

**Проверка:** запустить бота, отправить `/start` и обычное сообщение, получить корректные ответы.

---

## Итерация 2 — Единая конфигурация
- [x] Параметры бота и модели читаются из окружения
- [x] Бот стартует только с валидной конфигурацией

**Проверка:** запуск с корректными переменными проходит, с некорректными — понятная ошибка при старте.

---

## Итерация 3 — Текст: извлечение и сохранение транзакции
- [x] Бот извлекает из текста структурированную транзакцию
- [x] Сохраняются ключевые поля: направление, сумма, категория, тип траты, описание
- [x] Пользователь получает подтверждение о сохранении

**Проверка:** отправить несколько текстовых записей, убедиться, что бот подтверждает каждую.

---

## Итерация 4 — Фото чека: извлечение и сохранение
- [x] Бот принимает фото чека
- [x] По фото создается структурированная запись с подтверждением пользователю

**Проверка:** отправить фото чека и получить подтверждение, что запись добавлена.

---

## Итерация 5 — Отчет по периоду и категориям
- [x] Бот формирует отчет: доходы, расходы и итоговый баланс
- [x] В отчете есть разрез по периоду и категориям

**Проверка:** добавить тестовые записи и запросить отчет за разные периоды, сверить агрегаты.

---

## Итерация 6 — Контекст последнего диалога
- [x] Бот учитывает контекст последних сообщений для уточнений пользователя
- [x] Уточнение корректно влияет на сохранение записи или ответ

**Проверка:** отправить исходную запись и затем уточнение, убедиться, что бот отвечает в нужном контексте.

---

## Итерация 7 — Устойчивость и ошибки
- [x] Бот корректно обрабатывает невалидный ввод
- [x] При сбоях внешних сервисов возвращает безопасный ответ

**Проверка:** отправить неподдерживаемый ввод и смоделировать сбой, бот не падает и отвечает предсказуемо.

---

## Итерация 8 — Финализация MVP
- [x] Проверен полный пользовательский путь от первой записи до отчета
- [x] Обновлены инструкции запуска и проверки

**Проверка:** пройти сценарий "с нуля" по инструкции и подтвердить стабильную работу.
