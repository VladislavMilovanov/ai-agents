# План разработки

Источник требований: [vision.md](vision.md).

---

## Прогресс

| Итерация | Название | Статус | Дата |
|:--------:|----------|:------:|:----:|
| 0 | Каркас проекта | ✅ | 2026-05-26 |
| 1 | Конфигурация и запуск | ✅ | 2026-05-26 |
| 2 | Telegram и `/start` | ✅ | 2026-05-26 |
| 3 | Диалог с LLM | ✅ | 2026-05-26 |
| 4 | История в памяти | ✅ | 2026-05-26 |
| 5 | Граничные случаи | ✅ | 2026-05-26 |
| 6 | Docker и запуск | ✅ | 2026-05-26 |

**Легенда:** ⬜ не начато · 🔄 в работе · ✅ готово

**Текущая итерация:** MVP завершён

---

## Итерация 0 — Каркас проекта

> **Проверка:** `make install` и `make lint` проходят без ошибок.

- [x] Зависимости (`pyproject.toml`, `uv.lock`), Makefile, `.gitignore`
- [x] Структура каталогов `app/` по vision
- [x] `.env.example` без секретов
- [x] Точка входа `python -m app` (пока без логики бота)

---

## Итерация 1 — Конфигурация и запуск

> **Проверка:** приложение стартует с валидным `.env`; при пустых обязательных полях — понятная ошибка и выход.

- [x] Загрузка настроек из env (`Settings`)
- [x] Логирование при старте (`LOG_LEVEL`)
- [x] Сборка зависимостей в `main.py`

---

## Итерация 2 — Telegram и `/start`

> **Проверка:** бот в личном чате отвечает на `/start` приветствием; polling работает.

- [x] Подключение к Telegram (long polling)
- [x] Команда `/start` — приветствие без сброса истории
- [x] Только личные чаты (`private`)

---

## Итерация 3 — Диалог с LLM

> **Проверка:** текстовое сообщение → ответ ассистента по роли из `SYSTEM_PROMPT`.

- [x] Запрос к OpenRouter, ответ в чат
- [x] Индикатор «печатает…» на время ожидания
- [x] Слои: handler → `DialogService` → LLM-клиент
- [x] Заглушка `ContextProvider` (без RAG)

---

## Итерация 4 — История в памяти

> **Проверка:** бот помнит контекст в рамках чата; после рестарта — «с чистого листа».

- [x] Хранение диалога по `chat_id` в RAM
- [x] Учёт системного промпта и последних N пар сообщений
- [x] Несколько сообщений подряд — ответы согласованы с историей

---

## Итерация 5 — Граничные случаи

> **Проверка:** некорректный ввод и сбои обрабатываются предсказуемо.

- [x] Не текст (стикер, фото…) — вежливый отказ
- [x] Пустой текст — подсказка написать вопрос
- [x] Ошибка LLM / таймаут — короткое сообщение пользователю, детали в лог
- [x] Длинный диалог — в запрос уходят только последние N пар

---

## Итерация 6 — Docker и запуск

> **Проверка:** бот работает через `make docker-run` или `make up`; README описывает локальный и серверный запуск.

- [x] Dockerfile и `docker-compose.yml`
- [x] Команды Make для Docker
- [x] README: установка, `.env`, `make run`, Docker, пример systemd

---

## После MVP (вне плана)

RAG, `/reset`, `/help`, pytest, групповые чаты — по [vision.md](vision.md), отдельными итерациями.
