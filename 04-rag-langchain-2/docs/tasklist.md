# План разработки

Источник требований: [vision.md](vision.md).

---

## Прогресс

### Спринт 1 — Telegram + диалог + LLM (завершён)

| Итерация | Название | Статус | Дата |
|:--------:|----------|:------:|:----:|
| 0 | Каркас проекта | ✅ | 2026-05-26 |
| 1 | Конфигурация и запуск | ✅ | 2026-05-26 |
| 2 | Telegram и `/start` | ✅ | 2026-05-26 |
| 3 | Диалог с LLM | ✅ | 2026-05-26 |
| 4 | История в памяти | ✅ | 2026-05-26 |
| 5 | Граничные случаи | ✅ | 2026-05-26 |
| 6 | Docker и запуск | ✅ | 2026-05-26 |

### Спринт 2 — RAG (LangChain, InMemory)

| Итерация | Название | Статус | Дата |
|:--------:|----------|:------:|:----:|
| 7 | LangChain и конфиг | ✅ | 2026-05-26 |
| 8 | Индексация и команды | ✅ | 2026-05-26 |
| 9 | RAG-цепочка | ✅ | 2026-05-26 |
| 10 | Диалог и интеграция | ✅ | 2026-05-26 |
| 11 | Тесты и документация | ✅ | 2026-05-26 |

**Легенда:** ⬜ не начато · 🔄 в работе · ✅ готово

**Текущая итерация:** Спринт 2 завершён

---

## Спринт 1 (архив)

<details>
<summary>Итерации 0–6 — чеклисты</summary>

### Итерация 0 — Каркас проекта

> **Проверка:** `make install` и `make lint` проходят без ошибок.

- [x] Зависимости (`pyproject.toml`, `uv.lock`), Makefile, `.gitignore`
- [x] Структура каталогов `app/` по vision
- [x] `.env.example` без секретов
- [x] Точка входа `python -m app`

### Итерация 1 — Конфигурация и запуск

> **Проверка:** приложение стартует с валидным `.env`; при пустых обязательных полях — понятная ошибка и выход.

- [x] Загрузка настроек из env (`Settings`)
- [x] Логирование при старте (`LOG_LEVEL`)
- [x] Сборка зависимостей в `main.py`

### Итерация 2 — Telegram и `/start`

> **Проверка:** бот в личном чате отвечает на `/start` приветствием; polling работает.

- [x] Подключение к Telegram (long polling)
- [x] Команда `/start` — приветствие без сброса истории
- [x] Только личные чаты (`private`)

### Итерация 3 — Диалог с LLM

> **Проверка:** текстовое сообщение → ответ ассистента по роли из `SYSTEM_PROMPT`.

- [x] Запрос к OpenRouter, ответ в чат
- [x] Индикатор «печатает…» на время ожидания
- [x] Слои: handler → `DialogService` → LLM-клиент
- [x] Заглушка `ContextProvider` (без RAG)

### Итерация 4 — История в памяти

> **Проверка:** бот помнит контекст в рамках чата; после рестарта — «с чистого листа».

- [x] Хранение диалога по `chat_id` в RAM
- [x] Учёт системного промпта и последних N пар сообщений
- [x] Несколько сообщений подряд — ответы согласованы с историей

### Итерация 5 — Граничные случаи

> **Проверка:** некорректный ввод и сбои обрабатываются предсказуемо.

- [x] Не текст — вежливый отказ
- [x] Пустой текст — подсказка
- [x] Ошибка LLM / таймаут — короткое сообщение, детали в лог
- [x] Длинный диалог — только последние N пар

### Итерация 6 — Docker и запуск

> **Проверка:** бот через `make docker-run` или `make up`; README описывает запуск.

- [x] Dockerfile и `docker-compose.yml`
- [x] Команды Make для Docker
- [x] README: установка, `.env`, `make run`, Docker, systemd

</details>

---

## Спринт 2 — RAG

### Итерация 7 — LangChain и конфиг

> **Проверка:** `make install` и `make lint` проходят; `Settings` валидирует новые поля RAG.

- [x] Зависимости LangChain, pypdf в `pyproject.toml`
- [x] `Settings`: `EMBEDDING_MODEL`, `RETRIEVER_K`, `DATA_DIR`, `CHUNK_SIZE`, `CHUNK_OVERLAP`
- [x] Обновить `.env.example` (без секретов)
- [x] `SYSTEM_PROMPT` — роль **ассистента по продуктам Сбера** (пример в `.env.example`)

### Итерация 8 — Индексация и команды

> **Проверка:** после старта и `/index` команда `/index_status` показывает ненулевое число чанков.

- [x] `CorpusLoader`: PDF + JSON из `data/`
- [x] `IndexService`: чанки, эмбеддинги OpenRouter, `InMemoryVectorStore`
- [x] Полная переиндексация при **старте** в `main.py`
- [x] Команды **`/index`**, **`/index_status`** (`IndexCommandsHandler`)
- [x] В Docker-образ копируется `data/`

### Итерация 9 — RAG-цепочка

> **Проверка:** вопрос по содержимому корпуса → ответ с опорой на чанки; уточнение меняет поисковый запрос.

- [x] `RagChain`: query transformation → retriever (`RETRIEVER_K`) → финальный LLM
- [x] Этапы согласованы с **`rag_query_transform_chain`** / `naive-rag.ipynb`
- [x] Системные инструкции: опора на контекст, без выдуманных фактов

### Итерация 10 — Диалог и интеграция

> **Проверка:** диалог в Telegram через RAG; история в LangChain messages; команды индекса не сбрасывают историю.

- [x] `ChatHistory` на `HumanMessage` / `AIMessage`
- [x] `DialogService` → `RagChain` (без `OpenRouterClient` и `ContextProvider`)
- [x] Удалить `app/llm/openrouter_client.py`, `context_provider.py`, `models/chat_message.py`
- [x] Приветствие `/start` — помощник по продуктам Сбера

### Итерация 11 — Тесты и документация

> **Проверка:** `make test` и `make lint` зелёные; README — запуск RAG-бота с нуля.

- [x] `make test` → pytest (smoke индекса, `RagChain` с моками)
- [x] README: `data/`, env, `/index`, `/index_status`, диалог
- [x] Финальный `.env.example` по vision §9
